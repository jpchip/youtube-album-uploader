var chai = require('chai');var expect = chai.expect;var fileExists = require('../src/fileExists');describe('fileExists', function() {	it('should return true if file found', function () {		expect(fileExists('test/anchor.mp3')).to.be.true;	});		it('should return false if file not found', function () {		expect(fileExists('test/somethingsomething.something')).to.be.false;	});});